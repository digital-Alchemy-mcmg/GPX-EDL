# Contribution Rules

- Work from an accepted issue or bounded work order.
- Do not introduce new domains, module authority, or navigation by implementation convenience.
- Add or update contract tests with every boundary change.
- Keep candidate architecture out of canonical runtime files until accepted.
- Use small commits that identify the affected module and contract.
